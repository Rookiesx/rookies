server.port=8080
server.ssl.enabled=true
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=password
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=messaging-ssl
spring.application.name=realtime-messaging-app
# File Upload Limits
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# JPA / DataSource (MySQL)
spring.datasource.url=jdbc:mysql://mysql-17990714-rookies1504-0c7e.i.aivencloud.com:20591/defaultdb?sslMode=REQUIRED
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_********
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=false

# To use H2 in-memory instead, uncomment below:
# spring.datasource.url=jdbc:h2:mem:realtime;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

# To use Postgres, set these environment variables or override in application-prod.properties
# spring.datasource.url=jdbc:postgresql://localhost:5432/realtime
# spring.datasource.username=postgres
# spring.datasource.password=secret

# MySQL example (uncomment and set values to use MySQL)
# spring.datasource.url=jdbc:mysql://localhost:3306/realtime?useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=secret
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Optimizations
# Enable GZIP Compression
server.compression.enabled=true
server.compression.mime-types=text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json
server.compression.min-response-size=1024

# Enable Static Resource Caching (1 year)
spring.web.resources.cache.period=31536000

# HikariCP Connection Pool settings
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=20000
